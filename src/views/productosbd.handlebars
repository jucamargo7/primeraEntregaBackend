<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/css/styleproduct.css">
</head>
<body>  
    <div class="container">
        <h1>Bienvenido, {{user.first_name}}</h1>
        <p>Aquí encontrarás la lista de productos disponibles:</p>
        <div class="product-list">
            {{#each product}}
            <div class="product">
                <h2>{{this.title}}</h2>
                <p><strong>ID:</strong> {{this._id}}</p>
                <p><strong>Descripción:</strong> {{this.description}}</p>
                <p><strong>Código:</strong> {{this.code}}</p>
                <p><strong>Precio:</strong> {{this.price}}</p>
                <p><strong>Estatus:</strong> {{this.status}}</p>
                <p><strong>Stock:</strong> {{this.stock}}</p>
                <p><strong>Categoría:</strong> {{this.category}}</p>
                <button class="add-button" onclick="agregarProducto('{{this._id}}')">Agregar</button>
            </div>
            {{/each}}
        </div>
         <button class="final-button" onclick="continuarCompra()">Continuar compra</button>
        <input id="idCarrito" type="hidden" value="{{carritoId}}">
    </div>
    <script src="/js/productosbd.js"></script>
</body>
</html>









